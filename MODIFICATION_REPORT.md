# Amorist 修改与测试报告

生成日期：2026-07-24

## 1. 修改范围与项目结构

- 本次以 ZIP 中的外层 `amorist/` 为唯一源码基准。
- 实际上传包中未发现 `amorist/amorist_personal_site_dual_launcher/` 旧副本，因此没有从旧项目复制代码，也没有需要删除的内层目录。
- 原始上传 ZIP 未被覆盖；修改在独立工作副本中完成。
- `serve.ps1`、`serve.mjs`、全部启动 BAT/COMMAND 文件均保留，端口仍为 `4173`；仅在两套服务器的 MIME 表中补充 `.gif → image/gif`，以支持一键发布后保留的动态 GIF。
- 未增加 npm、Node 构建依赖、线上服务或框架。

## 2. 修改文件清单

| 文件 | 修改目的 |
|---|---|
| `assets/js/data-model.js` | 新增统一数据模型；集中处理游戏分类、角色喜好、角色身份及旧值兼容。 |
| `assets/js/amorist-app.js` | 接入稳定 ID、双维度角色筛选与批量操作、Bangumi 身份映射、手动身份保护及旧数据迁移。 |
| `assets/js/editor-tools.js` | 实现递归隐私过滤、本地一键生成、图片哈希/压缩/去重、manifest、失败回退及完整私人备份/恢复。 |
| `assets/js/oshi-hub.js` | 推角展示改用新的角色喜好字段，同时兼容旧 `relation`。 |
| `assets/js/public-mode.js` | 公开页锁定新增的角色批量编辑按钮。 |
| `editor.html` | 游戏分类稳定 ID；新增角色身份/喜好双筛选、双批量操作和编辑字段；加载统一数据模型。 |
| `index.html` | 与公开展示所需的新分类、角色字段和筛选结构同步；加载统一数据模型。 |
| `assets/css/amorist.css` | 为角色双筛选、身份标签和双批量操作补充必要布局。 |
| `assets/css/site-modes.css` | 为一键生成状态和统计报告补充必要样式。 |
| `serve.mjs` | 保留原 Node 启动方式和 4173 端口，仅补充 GIF MIME。 |
| `serve.ps1` | 保留原免 Node PowerShell 启动方式和 4173 端口，仅补充 GIF MIME。 |
| `MODIFICATION_REPORT.md` | 本说明、测试结论、已知限制及回退方法。 |

未修改 `data/amorist-data.json` 中的原始用户数据。首次打开 Editor 时只在内存中兼容读取与补齐；不会因为单纯打开页面就重写原 localStorage。角色在编辑、批量操作、Bangumi 同步或发布时写入新结构，游戏分类在保存或发布时写成稳定 ID。

## 3. 数据迁移规则

### 3.1 游戏分类

| 旧值 | 新内部 ID | 新显示文字 |
|---|---|---|
| 最推し | `hall_of_fame` | 殿堂入り |
| 好印象 | `love` | 大好き |
| 好き | `like` | 好き |
| 普通 | `normal` | 普通 |
| 苦手 | `difficult` | 苦手 |
| 未分類 | `unclassified` | 未分類 |

旧日文值仍可读取；新保存和新发布数据使用稳定 ID。

### 3.2 角色喜好 `preference`

| 旧值 | 新内部 ID | 显示文字 |
|---|---|---|
| 最推し | `favorite` | 最推し |
| 推し | `oshi` | 推し |
| 好き | `like` | 好き |
| 興味なし | `excluded` | 対象外 |
| 微妙 | `difficult` | 苦手 |
| 未分類 | `unclassified` | 未分類 |
| 无旧值对应 | `normal` | ふつう |

旧 `relation` 只作为兼容输入；新逻辑不再用它同时表达身份和喜好。

### 3.3 角色身份 `roleType`

| Bangumi relation | 新内部 ID | 显示文字 |
|---|---|---|
| 主角 | `route` | 攻略対象 |
| 配角 | `sub` | サブキャラ |
| 无关系、无法匹配或其他关系 | `unset` | 未設定 |
| 用户手动设置 | `protagonist` / 其他所选 ID | 主人公 / 对应文字 |

身份来源写入 `roleTypeSource`：

- `bangumi`：由项目现有 `sourceCharacters` 或 `data/bangumi-games.json` 映射。
- `manual`：用户确实修改过身份后写入；仅修改备注、图片、喜好等内容不会把 Bangumi 身份误标为手动。
- 后续 Bangumi 同步可更新头像、声优、别名、生日等资料，但不会覆盖 `roleTypeSource: "manual"` 的身份。

当前数据验证结果：83 部游戏、808 个角色；结合正式游戏库和 `bangumi-games.json` 后，376 个映射为攻略対象、417 个映射为サブキャラ、15 个因关系缺失或不属于“主角/配角”保持未設定。

## 4. 一键生成发布数据

1. 使用 `start-editor.bat` 或现有其他启动器，通过 `http://localhost:4173/editor.html` 打开 Editor。
2. 点击顶部原有的“站点数据”入口。
3. 点击“连接项目文件夹”。
4. 在浏览器选择器中直接选择外层 `amorist/` 根目录；该目录应包含 `editor.html`、`index.html`、`assets/` 和 `data/`。
5. 点击“一键生成发布数据”。

生成过程会：

- 递归排除任意层级 `visibility === "private"` 的整个对象；
- 在所有未知或新增字段中递归识别 `idb-image:` 和 `data:image/`；
- 从 IndexedDB 读取图片实体；
- 按原图 SHA-256 内容哈希生成稳定文件名；
- 普通静态图片转为最长边不超过约 2000 px、质量约 88% 的 WebP；
- GIF、APNG、动态 WebP及含动画元素的 SVG保留原动画，不静态化；
- 同内容图片只写一份；
- 写入 `assets/user-media/`；
- 将 JSON 中图片替换成 `assets/user-media/xxx.ext` 相对路径；
- 写入 `data/amorist-data.json` 和 `data/amorist-media-manifest.json`；
- 列出可能未使用的旧图片，但不删除或移动它们；
- 显示公开对象、排除 private、图片新增/复用/未使用/无法读取及 JSON 大小统计。

发布 JSON 会先写入临时文件并重新解析验证。最终写入失败时会尝试恢复原有 JSON 和 manifest；已经成功新增的图片可能作为未引用文件保留，不会误删原图。

## 5. 私人完整备份与恢复

在“站点数据”窗口中：

- 点击“导出私人完整备份”生成 JSON 备份。
- 备份包含当前站点 origin 下除临时公开预览外的全部 localStorage，以及实际枚举到的 Amorist / REPO Maker IndexedDB 数据库、store、key、Blob、File、ArrayBuffer 和图片实体。
- 当前确认的数据库包括：`otomeRepoMakerArchives`、`amorist-image-store-v1`、`amorist-bangumi-db`；代码会优先动态枚举，不只写死这三个名称。
- 点击“导入并恢复私人备份”时，系统会先下载当前状态安全备份，再执行恢复。
- 新版完整备份会精确恢复 localStorage；旧版备份采用兼容合并方式，不会因缺少新版字段而拒绝导入。

建议第一次使用时先导出备份，并在少量测试数据上完成一次“备份 → 修改 → 恢复”人工验证，再将其作为长期备份流程。

## 6. 测试报告

状态说明：**通过**为本环境已验证；**人工验证**表示依赖 Windows、浏览器权限、真实用户 IndexedDB 或视觉交互，不能虚构自动通过；**不适用**表示上传包不包含对应对象。

| # | 测试项 | 结果 |
|---:|---|---|
| 1 | ZIP 可正常解压 | 通过 |
| 2 | 内层旧项目被识别且不作为源码 | 不适用：上传 ZIP 中不存在该目录 |
| 3 | 外层正式项目可启动 | 通过：Node 启动器在 4173 提供 HTTP 200 |
| 4 | PowerShell 免 Node 启动器可继续使用 | 启动方式和端口未变，仅补 GIF MIME；需 Windows 人工启动验证 |
| 5 | Node 启动器文件仍存在 | 通过：4173 启动及 HTTP/MIME 检查通过 |
| 6 | Editor 页面可加载 | HTTP/静态资源通过；完整交互需人工验证 |
| 7 | Public 页面可加载 | HTTP/静态资源通过；完整交互需人工验证 |
| 8 | JavaScript 全部通过语法检查 | 通过：`node --check` |
| 9 | HTML 引用的本地资源都存在 | 通过 |
| 10 | 旧 `amorist-data.json` 可读取 | 通过：JSON 解析成功 |
| 11 | 最推し → 殿堂入り | 通过：映射单元测试 |
| 12 | 好印象 → 大好き | 通过：映射单元测试 |
| 13 | 游戏重新保存后分类不丢失 | 保存逻辑与归一化测试通过；界面操作需人工验证 |
| 14 | 游戏筛选和分组正常 | 稳定 ID 分组逻辑通过静态/数据测试；界面需人工验证 |
| 15 | 興味なし → 対象外 | 通过：映射单元测试 |
| 16 | 微妙 → 苦手 | 通过：映射单元测试 |
| 17 | 旧最推し、推し、好き不丢失 | 通过：808 条实际数据归一化计数一致 |
| 18 | 新增ふつう可保存和筛选 | 代码与选项完整；人工验证 |
| 19 | 対象外默认隐藏或折叠 | 通过：默认过滤逻辑已验证；视觉需人工验证 |
| 20 | 対象外可通过筛选查看 | 代码与筛选值完整；人工验证 |
| 21 | 角色身份筛选正常 | 代码与 DOM 完整；人工验证 |
| 22 | 身份和喜好组合筛选 | 组合过滤逻辑通过静态检查；人工验证 |
| 23 | Bangumi 主角 → 攻略対象 | 通过：实际数据与单元测试 |
| 24 | Bangumi 配角 → サブキャラ | 通过：实际数据与单元测试 |
| 25 | 无法匹配 → 未設定 | 通过：15 条保持未設定 |
| 26 | 手动攻略対象可改为主人公并保存 | 手动来源逻辑通过单元测试；人工操作验证 |
| 27 | 再同步不覆盖手动主人公 | 通过：手动优先单元测试与同步分支检查 |
| 28 | 路线角色候选逻辑不变 | 原 Bangumi `sourceCharacters` 路线逻辑未替换；人工回归验证 |
| 29 | 游戏详情主角/配角展示不变 | 原 `characterRoleGroup` 逻辑保留；人工回归验证 |
| 30 | 角色图片、备注、声优、所属作品不丢失 | 迁移只新增/归一化三个字段；实际字段计数和非破坏性测试通过 |
| 31 | 批量修改喜好 | 代码与 DOM 完整；人工验证 |
| 32 | 批量修改身份 | 代码与 DOM 完整；人工验证 |
| 33 | 一键生成覆盖公开 JSON | File System Access 写入逻辑和 HTTP 结构通过；真实文件夹授权需人工验证 |
| 34 | 生成失败不损坏旧 JSON | 通过：模拟写入失败及回滚单元测试 |
| 35 | 新图片生成独立 WebP | 转换逻辑完整；真实浏览器图片编码需人工验证 |
| 36 | 相同图片不重复生成 | 通过：实际转换函数去重单元测试 |
| 37 | JSON 图片路径为相对路径 | 通过：生成路径单元测试 |
| 38 | Public 加载生成后的图片 | 资源路径规则正确；真实生成后人工验证 |
| 39 | 深层 private 对象排除 | 通过：递归过滤单元测试 |
| 40 | private 图片不写入公开目录 | 通过：先过滤后转换顺序及单元测试 |
| 41 | 未知图片字段递归识别 | 通过：未知嵌套字段单元测试 |
| 42 | 不再引用图片不自动删除 | 通过：只生成 `possiblyUnused` 清单 |
| 43 | 生成结果有统计 | 通过：报告字段和 UI 已实现 |
| 44 | 私人备份包含实际 Blob | 通过：Blob 序列化/反序列化单元测试 |
| 45 | 恢复后图片不丢失 | 数据恢复逻辑通过单元测试；真实用户 IndexedDB 往返需人工验证 |
| 46 | REPO Maker 正常 | 未改其主体逻辑；人工回归验证 |
| 47 | 动漫库正常 | 未改其分类体系；角色写回兼容新结构；人工回归验证 |
| 48 | 创作工坊正常 | 未改主体逻辑；人工回归验证 |
| 49 | 主题切换正常 | 21 套主题定义未改；公开标量配置修复；人工验证 |
| 50 | 手机端无明显错位 | CSS 静态检查通过；人工视觉验证 |
| 51 | 电脑端无明显错位 | CSS 静态检查通过；人工视觉验证 |
| 52 | 最终 ZIP 删除内层旧项目 | 通过：最终包中不存在该目录 |
| 53 | 最终 ZIP 保留外层正式项目 | 通过 |
| 54 | 无 node_modules/缓存/无关日志 | 通过打包检查 |
| 55 | 原始数据和回退副本未覆盖 | 通过：原 ZIP 校验保留，工作副本独立 |

附加验证：

- HTML ID 无重复。
- 两个修改 CSS 文件的大括号、字符串和注释结构平衡。
- 游戏实际分类归一化计数：`hall_of_fame 6`、`love 14`、`like 9`、`normal 12`、`difficult 6`、`unclassified 36`。
- 角色实际喜好归一化计数：`favorite 1`、`oshi 12`、`unclassified 795`，未擅自把旧角色放入 `ふつう`。
- 递归图片测试验证了内容哈希命名、相同图片复用、未知字段识别、GIF/APNG/动态 WebP 保护和相对路径；Node 本地服务器的 GIF 响应类型为 `image/gif`。
- Chromium 在本测试容器中连简单 data URL 都无法正常退出，因此未将其作为浏览器交互测试依据，也未虚构截图或交互通过。

## 7. 已知限制

1. File System Access API 必须在 Chrome/Edge/Chromium、localhost 环境中由用户点击触发；Firefox/Safari 不支持该写入流程。
2. 浏览器不提供真正的跨文件原子重命名。本实现采用临时文件解析验证、最终覆盖和旧文件回写；若浏览器或系统在极端情况下同时中断，仍应使用原始 ZIP 或私人备份回退。
3. 生成过程中若图片已经写入、随后 JSON 失败，新图片可能留下成为“未使用图片”，但旧 JSON 不应被替换；系统不会自动删除它们。
4. 私人备份可能很大，因为图片实体会写入 JSON。备份数据若包含循环引用会明确报错，而不会生成不完整备份；当前项目数据结构未发现需要循环引用。
5. 15 个角色的 Bangumi relation 缺失、属于“闲角”或没有可关联的乙女游戏记录，因此按规则保留未設定，不进行主观推断。
6. Windows PowerShell 启动、真实文件夹授权、真实 IndexedDB 恢复、Bangumi 在线同步及移动/桌面视觉布局仍需用户在实际电脑上完成最终人工回归。

## 8. 回退方法

### 完整回退

直接重新解压原始上传的 `amorist(1).zip`，替换修改版项目。原始上传文件未被覆盖。

### 数据回退

1. 在修改前或日常使用中导出“私人完整备份”。
2. 打开修改版 Editor 的“站点数据”。
3. 点击“导入并恢复私人备份”。
4. 系统会先下载恢复前安全备份，再恢复 localStorage 和 IndexedDB。

### 仅公开数据回退

将之前正常的 `data/amorist-data.json` 和 `data/amorist-media-manifest.json`（如存在）复制回项目 `data/`。`assets/user-media/` 中多余图片可暂时保留，不影响旧 JSON。

## 9. 补充修复：公开页开放创作工具（2026-07-24）

根据实际使用定位，`index.html` 中的以下两个模块属于提供给公开访客使用的本地创作工具，不应套用公开页只读限制：

- 游戏 REPO / 乙女游戏 REPO Maker
- 图文创作（九宫格、Tier、偏好卡等）

本次补充修复：

- 公开页的 REPO 输入、图片选择与裁切、换肤、长评页、清空、截图、存档、新建、导入和导出恢复为可操作状态；
- 公开页的图文创作标题、图片格、文字、保存、清空和导出恢复为可操作状态；
- 不再把游戏卡片上的“写 REPO”改成“查看 REPO”；
- 游戏档案、角色图鉴、动漫档案、自介及其他网站内容管理功能仍保持只读；
- 访客的 REPO、主题设置、图文作品和每部游戏的 REPO 映射写入独立的浏览器本地存储命名空间；
- 公开页 REPO 的存档库和图片库使用独立 IndexedDB，不会与同源 Editor 的数据库串库；
- `index.html` 与 `editor.html` 的 REPO 和图文创作 DOM 已核对为一致，无需修改两份 HTML。

补充修改文件：

- `assets/js/public-mode.js`
- `assets/js/public-bootstrap.js`
- `assets/js/image-store.js`
- `assets/js/amorist-app.js`
- `assets/css/site-modes.css`

补充测试：

- 上述 JavaScript 均通过 `node --check`；
- `index.html` 中 `studio` 与 `forms` 两个模块的 DOM 与 Editor 完全一致；
- 公开页不再锁定这两个模块的输入控件，也不再隐藏其操作按钮和图片占位入口；
- 访客工具 localStorage、REPO 存档 IndexedDB、图片 IndexedDB 的隔离规则通过静态断言；
- Node 本地服务器启动后，`index.html` 及相关 JavaScript/CSS 均可正常返回；
- 最终点击、图片上传、存档恢复和移动端视觉效果仍建议在实际 Chrome/Edge 中人工回归一次。
