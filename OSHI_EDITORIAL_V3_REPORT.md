# Amorist 推角个人页 Editorial V3 修改与测试报告

## 1. 基准与范围

- 唯一代码基准：本次上传的 `amorist_oshi_magazine_complete(1).zip`。
- 项目结构保持为原生 HTML / CSS / JavaScript 多文件项目。
- 未引入 React、Vue、Vite、npm 依赖、构建工具、在线服务或第三方排版库。
- 保留 `serve.ps1`、`serve.mjs` 及原有启动脚本。
- 本次仅修改推角展示层及其必要编辑体验；REPO Maker、动漫库、创作工坊、主题定义和其他模块未改动。

## 2. 真实数据检查

当前导出数据中：

- 推角记录：13 个。
- 图片集：9 个。
- 图片集图片：58 张。
- 比例分布：横图 44、竖图 10、近方图 4。
- 当前真实数据中没有宽高比大于等于 2.4 的极宽图，也没有宽高比小于等于 0.42 的极长竖图。
- 图片较多的角色：ドロシー 10 张、二角兽 14 张、伊孚 16 张、有村乃亜 18 张。
- 当前导出数据的图片集图片均可由导出 JSON 直接恢复；本次导出内没有 `idb-image:` 图片引用。

## 3. 修改文件

### `assets/js/oshi-hub.js`

目的：重写推角个人页图片排版决策和渲染，扩展编辑器控制，并保持旧数据兼容。

主要修改：

1. 新增五种底层排版模式：
   - Editorial Flow
   - Cinema Sequence
   - Gallery Spread
   - Original Masonry
   - Archive Grid
2. 根据图片集类型、图片数量、真实宽高比、主图和层级生成合格候选，不再按图片数量直接套固定格子。
3. 新增自然杂志流分段、比例行排版、画册式段落和原比例瀑布流渲染。
4. 默认图片均使用自然高度；只有显式选择“铺满裁切”时才进入固定比例裁切容器。
5. 主图、重点、标准、辅助通过自然宽度、行内权重和段落位置控制视觉面积。
6. 多图自动拆段，避免固定五图模板无限重复。
7. Editor 新增图片集类型、当前排版、合格候选、换一个合格版式、主图、层级、显示方式和焦点控制。
8. CG 情节图注仍为可选；空内容不创建 DOM。
9. 人物引文仍为可选；关闭或空内容不创建 DOM。
10. 图片读取失败时显示明确错误提示，不再静默留下空框。
11. 新增幂等内容迁移版本：状态版本 4，推角内容版本 3。

### `assets/css/amorist.css`

目的：建立自然比例、无色块、非卡片化的个人作品集视觉系统，并重新组织移动端。

主要修改：

1. 删除上一版 `Oshi personal issue v2` 中的固定 1～5 图网格、固定高度、默认 cover、固定矩形容器和有色填充逻辑。
2. 新增 `Oshi personal issue v3` 局部样式，限定在推角个人页和编辑预览内。
3. 普通图片显式重置为 `aspect-ratio:auto`、`height:auto`、透明背景。
4. 仅 `[data-fit="cover"]` 使用固定比例和 `object-fit:cover`。
5. 重做人物页顶部四类封面结构的自然图片表现。
6. 重做 Editorial、Cinema、Gallery、Masonry、Archive 的间距和视觉节奏。
7. 手机端独立重排：390/375px 为单列语义顺序，横图使用可用宽度，未主动裁切的图片保持原比例。
8. 不新增渐变，不修改主题定义。

### `docs/oshi-editorial-v3/screenshots/`

保存本次真实数据桌面、手机和 Editor 截图。

## 4. 已删除或替换的旧逻辑

已删除上一版专门新增的以下视觉基础：

- `is-count-1` 至 `is-count-5` 固定网格模板。
- 多图统一 `height:100%`。
- 固定 `min-height` 版面。
- 默认 `object-fit:cover`。
- ARTWORKS / CG 共用固定角色位。
- 固定矩形容器中的主题色或灰色空块。
- 按五张一组切块后强行指定 hero / a / b / c / d 格位。

原有更早的基础样式仍保留用于兼容，但 V3 对个人页使用了更具体的局部规则并显式重置 `aspect-ratio`、高度、背景和图片适配。

## 5. 新排版算法

### 5.1 候选模式选择

1. 读取图片集类型或根据标题推断 CG、ARTWORKS、ARCHIVE。
2. 读取图片数量和全部宽高比。
3. 统计横图与竖图构成。
4. 根据类型和组成生成少量合格候选。
5. 自动模式只在候选中循环；手动选择不适合的模式会在 Editor 中禁用。

### 5.2 Editorial Flow

- 每段 1～3 张。
- 极宽、极竖、主图或重点图可独立成段。
- 竖图与横图混合时允许一竖配两横、错位和非对称留白。
- 不要求左右等高。

### 5.3 Cinema Sequence / Archive Grid

- 使用基于宽高比的 justified row。
- 每行 2～4 张。
- 宽度由图片比例乘以层级权重计算。
- 同一行图片自然等高，但不裁切。
- 主图可独立成行。
- 最后一行不强制拉满。

### 5.4 Gallery Spread

- 主图、重点图、极端比例图可单独承担一个段落。
- 其他图片以双联或错位段落出现。
- 允许明显留白，不用背景容器填空。

### 5.5 Original Masonry

- 原始比例瀑布流兜底。
- 不等高、不裁切、无背景色块。
- 手机端自动降为单列。

## 6. 数据迁移规则

- 状态版本：4。
- 内容版本：3。
- 明确保存为 `cover` 的图片继续保留裁切与焦点。
- 旧 `contain` 转为 `original`。
- 旧 `original` 保留为 `original`。
- 未明确或旧自动状态转为 `auto`，但 `auto` 在展示层仍不裁切。
- 新图片默认 `auto`。
- 新图片集默认 `layoutMode:auto`、`layoutVariant:0`。
- 图片 ID 沿用原值；缺失 ID 时用图片集 ID 与稳定顺序生成。
- 同一图片集只允许一个主图。
- 迁移完成后写入当前内容版本，后续加载不会重复迁移。

## 7. Editor 使用方法

### 图片集

- “图片集类型”：决定 CG、ARTWORKS、ARCHIVE 的视觉语言。
- “当前排版”：自动推荐或手动选择合格模式。
- “换一个合格版式”：只在当前图片组合支持的候选中切换。
- 图片集可拖动排序，也可用上下按钮排序。

### 图片

- “主图”：决定当前图片区段的视觉重心。
- “层级”：重点 / 标准 / 辅助，控制自然展示面积和位置。
- “显示”：
  - 自动构图：系统决定宽度和位置，不裁切。
  - 原图完整展示：明确保持原图完整。
  - 铺满裁切：使用固定裁切容器并显示横向、纵向焦点。
- 图片可拖动排序，也可用上下按钮排序。
- CG 图片才显示可选情节图注。

### 人物页封面

- 自动判断：根据真实比例选择竖版人物、横版电影或极简画册。
- 手动模式：竖版人物 / 横版电影 / 极简画册。
- 默认不裁切；选择“铺满裁切”后才显示焦点控制。

## 8. 自动测试结果

### 8.1 结构与启动

- ZIP 解压：通过。
- 所有 `assets/js/*.js` 语法检查：通过。
- `serve.mjs` 语法检查：通过。
- CSS 花括号结构：通过。
- HTML 本地资源引用：通过，无缺失。
- Node 启动器 HTTP 冒烟测试：`/`、`editor.html`、`index.html`、核心 JS、核心 CSS、数据 JSON 均返回 200。
- `serve.ps1`：文件保留且未修改；当前 Linux 测试环境没有 PowerShell，需在 Windows 公司电脑上人工启动确认。

### 8.2 数据与交互

浏览器自动化结果：

- 状态迁移为版本 4：通过。
- 推角内容迁移为版本 3：通过。
- 旧 `contain` 不再残留：通过。
- 图片 ID 唯一：通过。
- public 人物页加载：通过。
- editor 人物页加载：通过。
- 五种排版模式实际切换：通过。
- 图片排序保存：通过。
- 图片集排序保存：通过。
- 主图保存：通过。
- 层级保存：通过。
- 显示方式保存：通过。
- 横向、纵向焦点保存：通过。
- 人物页封面裁切保存和实时预览：通过。
- 空 CG 图注不生成 DOM：通过。
- 空人物引文不生成 DOM：通过。

### 8.3 图片数量与自然比例

使用项目真实图片建立 1、2、3、5、10 张图片测试数据：

- 1 张：通过。
- 2 张：通过。
- 3 张：通过。
- 5 张：通过。
- 10 张：通过。
- 所有测试图的展示宽高比与图片天然宽高比误差小于 0.03：通过。
- 普通图片计算样式 `aspect-ratio:auto`：通过。
- 普通图片 `object-fit:contain` 且容器透明：通过。

### 8.4 响应式

实际测试宽度：1440、1280、1024、768、390、375px。

- 六档宽度均无页面横向溢出。
- 六档宽度图片天然比例均保持。
- 390 / 375px 下 justified row 转为单列语义顺序。
- 手机端未出现自动 cover 或固定高度回归。

### 8.5 截图验收

已保存：

- `desktop-noa-full.png`：18 张混合图片，含 ARTWORKS、CG、OTHERS。
- `desktop-ifu-full.png`：16 张，竖图艺术画册与横向 CG。
- `desktop-unicorn-full.png`：14 张，CG 为主。
- `mobile-noa-full.png`：390px 长页面。
- `mobile-ifu-full.png`：375px 长页面。
- `editor-cover-crop-preview.png`：人物页封面裁切实时预览。
- `editor-image-sorting.png`：图片集候选、图片排序、主图、层级和显示方式。
- `editor-overview-crop-preview.png`：推角总览裁切预览。

截图检查未发现图片拉伸、普通图片意外裁切、有色空容器或横向溢出。

## 9. 未自动验证与已知限制

以下项目不能在当前受管测试环境中完整自动验证，必须明确保留为人工验收项：

1. **真实 IndexedDB 端到端读取**：当前导出数据没有 `idb-image:` 图片；相关读取函数、异步令牌和兼容路径均保留，但需用用户本机数据库人工验证。
2. **PowerShell 无 Node 启动**：当前环境没有 PowerShell；`serve.ps1` 未修改，需在 Windows 公司电脑上人工执行。
3. **极宽图与极长竖图真实视觉截图**：当前项目真实数据中没有这两类图片，不能伪造为“真实图片验收通过”。算法分支和自然比例规则已覆盖，需用户后续导入真实极端比例图片人工确认。
4. **21 套主题逐套人工审美验收**：主题定义和其他主题代码未修改，V3 只使用现有 CSS 变量；仍建议在用户常用深色、浅色、浓郁和淡雅主题下各人工浏览一次。
5. **REPO Maker、图文创作、一键发布、私人备份的完整业务流程**：代码文件未改动，资源与页面加载冒烟测试通过；涉及用户真实数据写入和导出的流程建议人工复核。

## 10. 回退方法

1. 关闭 Amorist 页面和本地服务器。
2. 将当前修改版文件夹移走或重命名。
3. 解压交付的 `amorist_oshi_magazine_before_editorial_v3.zip`。
4. 使用原有 `start-editor.bat`、`start-public.bat`、`serve.ps1` 或 Node 启动器启动。
5. 若仅回退本次代码，也可从回退包恢复：
   - `assets/js/oshi-hub.js`
   - `assets/css/amorist.css`

本次修改没有直接改写 `data/amorist-data.json`。
