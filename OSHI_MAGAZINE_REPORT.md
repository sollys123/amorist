# Amorist 推角个人展示页改造报告

生成日期：2026-07-24

## 1. 修改基准

- 以 `amorist_public_tools_fixed.zip` 为唯一修改基准。
- 保留上一版已经完成的公开页 REPO Maker / 图文创作工具开放逻辑。
- 未修改游戏库、角色图鉴、REPO Maker、动漫库、创作工坊、21 套主题及公开发布/私人备份逻辑。
- 原始 ZIP 未被覆盖，修改在独立工作副本中完成。

## 2. 修改文件

| 文件 | 修改内容 |
|---|---|
| `assets/js/oshi-hub.js` | 修复总览裁切预览；新增人物页封面自适应、图片与图片集排序、主图、视觉层级、裁切焦点、版式模板、CG 可选情节图注、可选人物引文和旧数据兼容。 |
| `assets/css/amorist.css` | 新增人物特刊式封面、CG/ARTWORK/ARCHIVE 排版语言、1～5 图模板、多段跨页、手机端重排、编辑器实时预览和克制的杂志化装饰。 |
| `OSHI_MAGAZINE_REPORT.md` | 本报告。 |

`editor.html` 和 `index.html` 的现有 DOM 已足够，新增编辑控件由 `oshi-hub.js` 在 Editor 中按需生成，因此没有重复修改两份 HTML。

## 3. 总览裁切预览修复

原问题是裁切预览直接将 `idb-image:...` 写入 `<img src>`，浏览器无法读取 IndexedDB 内部引用。

现在会：

1. 判断图片是否为 IndexedDB 引用；
2. 通过 `amoristImageStore.get()` 取得实际图片；
3. 显示“正在读取图片”“图片读取失败”等明确状态；
4. 使用请求令牌避免快速切换人物时旧图片异步覆盖新图片；
5. 横向焦点、纵向焦点和铺满/完整显示继续实时反映在预览中。

## 4. 人物页封面

新增四种封面选择：

- 自动判断；
- 竖版人物；
- 横版电影；
- 极简画册。

自动模式根据主视觉图片比例选择版式。另可设置：

- 自动、完整显示、铺满裁切；
- 横向焦点；
- 纵向焦点。

Editor 中新增与最终封面逻辑一致的实时预览。旧人物页没有这些字段时，默认采用自动版式、自动适配和 50% / 50% 焦点。

## 5. 图片集和图片控制

### 图片集

- 图片集可拖动排序；
- 同时提供上移/下移按钮，便于触屏或拖动不方便时使用；
- 可选择排版语言：自动、CG / 电影专题、ARTWORK / 艺术画册、ARCHIVE / 收藏档案；
- 每组提供 3 个适合当前素材的版式变体，可点击“换一个版式”；
- 多个跨页会自动交替版式，避免连续重复；
- 可选择是否显示人物引文，并自行填写内容；未启用或未填写时不生成占位。

### 单张图片

- 可拖动排序；
- 同时提供上移/下移按钮；
- 可指定本组主图；
- 可设置重点、标准、辅助三级视觉层级；
- 可设置自动、完整显示、铺满裁切；
- 可调整横向和纵向焦点；
- 编辑缩略图会实时显示当前裁切结果；
- 不恢复普通图片说明输入框。

三级视觉层级会参与自动主图和版面位置选择：重点图片优先获得较大的视觉位置，辅助图片优先进入较小的辅助格。

## 6. 自动排版规则

- 1～5 张图片分别有独立版式；
- 6 张以上自动拆成 3～5 张一组的多个跨页；
- 6 张拆为 3+3，7 张拆为 4+3，8 张拆为 4+4，11 张拆为 4+4+3；
- 不会出现多图情况下最后只剩 1 张或 2 张的机械孤立尾页；
- 系统综合图片顺序、指定主图、视觉层级、横竖比例和图片集类型选择版面位置；
- 未进行任何设置的旧图片集也能直接使用自动排版。

### CG

- 优先采用宽幅、分镜和主场景组合；
- 自动适配默认偏向铺满裁切；
- 每张图片可以填写“情节图注（可选）”；
- 图注为空时不显示、不预留空间，也不改变排版。

### ARTWORK

- 默认完整显示；
- 更强调竖图、主作品、大留白和画册式层级；
- 不显示普通图片说明。

### ARCHIVE

- 采用收藏档案式框架和混合比例组合，适合横图、竖图和方图混合素材。

## 7. 杂志化元素

新增元素保持克制，主要包括：

- 图片集序号；
- 跨页序号；
- 极细分隔线；
- 淡色大号编号；
- 可选人物引文；
- CG 可选情节图注。

没有增加花朵、爱心、蝴蝶、胶带、随机贴纸、无意义英文或新的渐变装饰。

## 8. 手机端

手机端不压缩桌面网格，而是：

- 按真实图片顺序单列重排；
- 横向 CG 保持宽幅比例；
- 避免狭窄双栏；
- 主图、重点图和图注关系仍然保留；
- 减少版面装饰；
- 编辑器改为更适合窄屏的单列控件。

## 9. 数据兼容

推角数据版本提升为 3，但旧数据无需手工迁移。

旧图片对象：

```json
{
  "src": "...",
  "w": 1280,
  "h": 720,
  "alt": "file.jpg"
}
```

读取时自动补齐：

```json
{
  "id": "cg-image-1",
  "priority": "standard",
  "fit": "auto",
  "focusX": 50,
  "focusY": 50,
  "hero": false,
  "sceneCaption": ""
}
```

图片集自动补齐：

```json
{
  "type": "auto",
  "layoutVariant": 0,
  "quoteEnabled": false,
  "quoteText": ""
}
```

人物页自动补齐：

```json
{
  "profileLayout": "auto",
  "profileFit": "auto",
  "profileX": 50,
  "profileY": 50
}
```

旧字段、图片内容、图片顺序和现有文字均保留。只有用户实际调整或保存后，相关记录才写入新增字段。

## 10. 已完成测试

- 全部 JavaScript 通过 `node --check`。
- `amorist.css` 通过 `tinycss2` 解析，无 CSS 语法错误。
- CSS 大括号数量平衡。
- `editor.html`、`index.html` 无重复 ID。
- 两份 HTML 引用的本地资源均存在。
- Editor、Public、JS、CSS 和数据文件通过本地服务器 HTTP 200 检查。
- 旧数据模型测试通过：旧图片自动补齐但不丢字段。
- 1～12 张图片分组算法测试通过。
- 实际公开数据测试通过：13 个推角记录、9 个图片集、58 张图片均可归一化，未产生缺失 ID 或重复 ID。
- CG / ARTWORK 自动识别测试通过。
- 主图优先、重点图优先、封面横竖比例判断测试通过。
- 新字段 JSON 保存后再次读取的幂等测试通过。
- 原 `amorist-data.json`、Bangumi 数据和其他模块文件未修改。

## 11. 需要人工验证

当前容器中的 Chromium 对 localhost 和 file URL 返回 `ERR_BLOCKED_BY_ADMINISTRATOR`，因此不能虚构浏览器交互和视觉截图已经通过。请在 Windows Chrome / Edge 中人工验证：

1. 推角总览 → 编辑图片：缩略图可见，调整焦点实时变化；
2. 打开任一人物页 → 编辑：封面预览可见；
3. 拖动图片和图片集并刷新页面，确认顺序保留；
4. 设置主图、重点/标准/辅助并切换 3 个版式；
5. 设置完整显示/铺满裁切和焦点；
6. CG 图注为空时页面无占位，填写后出现；
7. 人物引文关闭时无占位，打开后出现；
8. 桌面端与手机模拟模式检查布局。

## 12. 回退

完整回退：重新解压上一版 `amorist_public_tools_fixed.zip`。

局部回退：将上一版中的以下文件覆盖回来：

```text
assets/js/oshi-hub.js
assets/css/amorist.css
```
