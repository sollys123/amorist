# Oshi Editorial V3.1 — 角色分类轴与同步范围修正

## 修改内容

1. 角色图鉴的喜好轴现在会在以下身份分类中保留：
   - 全部
   - 主人公
   - 攻略対象
   - サブキャラ

   仅在“未設定”分类中不显示喜好轴。搜索状态或单独选择某个喜好筛选时，仍沿用原有普通卡片结果展示。

2. 推角展示的自动同步范围收紧为：
   - 身份必须为 `攻略対象（route）`；
   - 喜好必须为 `最推し（favorite）` 或 `推し（oshi）`。

   主人公、サブキャラ和未設定角色即使标记为推し或最推し，也不会进入推角展示。

## 修改文件

- `assets/js/amorist-app.js`
- `assets/js/oshi-hub.js`

## 兼容性

- 未修改角色、游戏或推角的数据结构。
- 未删除已有推角展示自定义记录；不符合新同步条件的角色只是不再显示。
- 当角色身份以后改为攻略対象，原有同 ID 展示资料仍可重新关联。
- public 与 editor 共用相同筛选逻辑。

## 验证

- 两个修改文件均通过 `node --check`。
- 已完成五种身份分类的轴显示条件真值测试。
- 已完成四种身份 × 三种喜好的推角资格组合测试。
- 当前随项目发布的真实数据中：原有 13 位推/最推角色，按新规则保留 8 位攻略対象，排除 4 位サブキャラ和 1 位未設定角色。
